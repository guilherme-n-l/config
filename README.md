# config files

## Needs

- Nix
- Valid arch: aarch64 or x86_64
- POSIX-like knowledge (aka Divine Intellect)

## Help

```sh
nix run .#install
```
> Installs dotfiles (make sure to place the clone config into ~/config)
